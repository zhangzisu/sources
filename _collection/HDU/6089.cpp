#include<cstdio>
#include<cstdlib>
#include<cstring>
#include<algorithm>
#define MAXN 100010
int t, n, m, k, q;
int main(){
    scanf("%d", &t);
    while(t--){
        scanf("%d%d%d%d", &n, &m, &k, &q);
        
    }
    return 0;
}