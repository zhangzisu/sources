#include <algorithm>
#include <cstdio>
#include <cstdlib>
#include <cstring>
#define MAXN 10000000
typedef long long lnt;
lnt n;
int m, p, q, f[21];

int main() {
	scanf("%lld%d%d%d", &n, &m, &p, &q);
	for (int i = 0; i <= q; i++) scanf("%d", f + i);
	//
	return 0;
}