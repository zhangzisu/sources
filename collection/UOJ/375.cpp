#include<cstdio>
#include<cstdlib>
#include<cstring>
#include<algorithm>
#include<vector>
int t;
long long n, k, ans;
std::vector<long long> V;

int main(){
    scanf("%d", &t);
    while(t--){
        scanf("%lld%lld", &n, &k);
        ans = 1;
        for(int i = 1;i < k;i++)V.push_back(i);
        
    }
    return 0;
}