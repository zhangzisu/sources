#include <algorithm>
#include <cstdio>
#include <cstdlib>
#include <cstring>
#include <vector>
#define MAXN 100010
#define MAXM 300010
int n, m;
int main() {
	scanf("%d%d", &n, &m);
	for (int i = 1, a, b, x, y; i <= n; i++) {
		scanf("%d%d%d%d", &a, &b, &x, &y);
		//
	}
	return 0;
}