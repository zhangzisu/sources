#include <algorithm>
#include <cstdio>
#include <cstdlib>
#include <cstring>
int n, m, val[1 << 20];
int main() {
	scanf("%d%d", &n, &m);
	for (int i = 0; i < 1 << n; i++) scanf("%1d", val[i]);
    
	return 0;
}